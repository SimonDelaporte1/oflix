{% extends 'base.html.twig' %}

{% block title %}Modifier une saison{% endblock %}

{% block admin_nav %}{{ include('back/_nav.html.html.twig', {'nav_active': 'season_index'}) }}{% endblock %}

{% block body %}
    <h1>Modifier une saison</h1>

    {{ include('back/season/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-primary" href="{{ path('back_season_index') }}">Retourner à la liste</a>

    {{ include('back/season/_delete_form.html.twig') }}
{% endblock %}
