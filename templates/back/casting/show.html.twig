{% extends 'base.html.twig' %}

{% block title %}Casting{% endblock %}

{% block body %}
    <h1>Casting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ casting.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ casting.role }}</td>
            </tr>
            <tr>
                <th>Ordre d'apparition</th>
                <td>{{ casting.creditOrder }}</td>
            </tr>
            <tr>
                <th>Acteur</th>
                <td>{{ casting.person.PersonFullName }}</td>
            </tr>
            <tr>
                <th>Film</th>
                <td>{{ casting.movie.title }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('back_casting_index') }}">Revenir à la liste</a>

    <a class="btn btn-primary" href="{{ path('back_casting_edit', {'id': casting.id}) }}">Modifier</a>

    {{ include('back/casting/_delete_form.html.twig') }}
{% endblock %}
